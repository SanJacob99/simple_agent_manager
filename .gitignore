node_modules
dist
.env
.env.*
*.local
*.tsbuildinfo
.worktrees
.tmp/
test-results/
storage/
# The broad `storage/` rule above is for the runtime data directory, but it
# also matches the `server/storage/` source tree. Re-include the source so
# new modules there can't be silently dropped from commits.
!server/storage/
!server/storage/**
logs/
settings.json
graph.json
provider-catalog*
notes/
.browser-profile/
.sam/
.tmp*
browser-screenshots/

# User-installed tools — loaded at runtime via filesystem scan.
# See docs/concepts/user-tools-guide.md. Remove this line to commit a
# shared team library of tools to the repo.
server/tools/user/

audio/